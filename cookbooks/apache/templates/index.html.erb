This is the webpage Configured from Chef from template after setting the role
This is the new version of the website.
        <br> Hostname : <%= node['hostname'] %>
        <br> IP       : <%= node['ipaddress'] %>
